#ifndef VIGENERE_CIPHER_H
#define VIGENERE_CIPHER_H

#include <string>
using namespace std;

string VigenereCipher(const std::string& text, const std::string& key, bool encrypt);

#endif // VIGENERE_CIPHER_H
